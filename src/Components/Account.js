import React from 'react';
import { View, Text } from 'react-native';

export const Account = () => {

    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>dom Account</Text>
        </View>
    )
}
